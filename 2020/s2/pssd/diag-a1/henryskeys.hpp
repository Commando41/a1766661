using namespace std;
#include <vector>
#include <string>

class Henryskey {

	public:
		int howMany(string S){
		vector<string, att>;
		int len = strlen(s);
		for(int i = 0; i < len; i++;){
			att.push_back(s);		
		}
		int i, d;
		int atlen = att.length();
		for( i = 0; i<len; i++){
			for(d = 0; d < len; d++){
							
			}			
		}
	}	
}
